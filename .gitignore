__pycache__/
*.pyc
*.pyo
__pycache__
*.egg-info/
*.egg
dist/
build/
node_modules/
.vite/

# 运行时产物
tmp/
*.log

# 环境变量 — 含真实密钥，禁止提交
provider.env
.env
.env.*

# 运行时数据
data_status/call_log/

# 虚拟环境
.venv/
venv/

# provider — 拉取后运行 setup.py 自动创建，不提交代码
provider/

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# 测试 / 临时
.pytest_cache/
*.tmp
/tmp/

# 用户配置 — 每个部署实例不同，setup.py 会生成空运行态配置，提交示例模板即可
config/config.json
config/api_keys.json
config/models.json

# 运行时生成的 session 密钥（自动创建，不提交）
config/.auth_secret

# 备份文件（不提交）
*.bak
